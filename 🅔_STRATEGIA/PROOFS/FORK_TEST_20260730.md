# FORK TEST — dowód PB-1 (fork-safety)

> Data: 2026-07-30 | Pakiet: **PB-1** z `PROOF_BOOST_PLAN_20260707.md` | Znaleziska: C24 🔴, C25, C26, C30
> Skrypt symulacji: pełna procedura `INIT.md` wykonana 1:1 na kopii do `/tmp` (scratchpad sesji).

---

## 1. Problem (test negatywny — stara procedura)

Fork wykonany **bez** `rm -rf .git`, dokładnie jak stary Krok 1:

```
commity odziedziczone: 39
remote: origin  git@github.com:damianjedryka39-create/TEMPLE_REPO_v2.git (fetch)
        origin  git@github.com:damianjedryka39-create/TEMPLE_REPO_v2.git (push)
branch: main
```

Znaczenie: nowy projekt commitował na historię szablonu, a **pierwszy `git push` w jego `sync_state` (CO_PILOT §2 krok 7) pisał do repo TEMPLE na GitHubie**. Ryzyko nieodwracalne — potwierdzone, nie teoretyczne.

---

## 2. Co zmieniono

| Plik | Zmiana | Znalezisko |
|------|--------|-----------|
| `INIT.md` Krok 1 | `rm -rf "${SCIEZKA_NOWEGO_REPO}/.git"` + czyszczenie markerów sesji (`.session_active`, `.reflect_done`) + bramka `[ ! -d .git ]` | C24, D22 |
| `INIT.md` Krok 2 | Weryfikacja placeholderów: lista 20 wzorców z §2 zamiast `grep -rn "{{" .` (meta-szablony przestają fałszywie failować) + `{{ALIAS}}` dodany do sed | C26, C25 |
| `INIT.md` **Krok 5.5 (nowy)** | Reset pamięci szablonu — LESSONS do sekcji `⚡ ŻELAZNE`, DECISIONS pusta tabela, PROOFS tylko przykład, ARCHIVE pusty + ucięcie wiszących odsyłaczy `→ L<N>` | decyzja Fi 2026-07-30 |
| `INIT.md` Krok 6 | Bramka `[ ! -d .git ]` przed `git init` + weryfikacja `git log`=1 / `git remote`=0 | C24 |
| `INIT.md` nagłówek + §4 | „12 parametrami" → „13"; zasady 3/4 zgodne z faktem (były sprzeczne z zawartością); nowa zasada 11 (odcięcie forka) | C30, C24 |
| `AGENTS.md:33` | `{{ALIAS}}` → `{{ALIAS_PROJEKTU}}` — jedyny placeholder poza sed-listą | C25 |

**Decyzja Fi (2026-07-30) o pamięci przy forku:** czysty start + zachowane `⚡ ŻELAZNE`. Nowy projekt nie dziedziczy 18 sesji cudzej historii, ale reguły komunikacji Fi działają od pierwszej minuty.

---

## 3. Wynik testu (procedura po naprawie)

| Bramka | Oczekiwane | Zmierzone | Wynik |
|--------|-----------|-----------|-------|
| `.git` po Kroku 1 | brak | brak | ✅ |
| Placeholdery §2 (20 wzorców) | 0 | 0 | ✅ |
| `git log --oneline \| wc -l` | 1 | 1 | ✅ |
| `git remote -v \| wc -l` | 0 | 0 | ✅ |
| LESSONS `## L<N>` | 0 | 0 | ✅ |
| LESSONS `⚡ ŻELAZNE` | 1 | 1 | ✅ |
| Wiszące odsyłacze `→ L<N>` | 0 | 0 | ✅ |
| DECISIONS `\| D<N>` | 0 | 0 | ✅ |
| PROOFS (poza `.gitkeep`) | 1 (EXAMPLE) | 1 | ✅ |
| ARCHIVE (poza `.gitkeep`) | 0 | 0 | ✅ |
| Hooki `.claude/hooks/` | 6 | 6 | ✅ |
| Skille `SKILL/*.md` | 21 | 21 | ✅ |
| `INIT.md` / `REPOSITORIES/` usunięte | NIE istnieją | NIE istnieją | ✅ |
| Markery sesji szablonu | 0 | 0 | ✅ |
| Alias podmieniony w AGENTS.md | ≥1 | 2 | ✅ |

**Zachowanie zamierzone (nie błąd):** GATE Kroku 4.5 zwraca `grep -c "{{" CONSTITUTION.md = 18` → **STOP**. Konstytucja musi być wypełniona ręcznie przez Fi przed dalszym bootstrapem — bramka działa zgodnie z projektem.

---

## 4. Uwagi dla następnego pakietu

- Weryfikacja z §2 obejmuje **20 wzorców**, nie 17 jak zakładał plan (plan liczył bez wariantów `{{ROLA}}`/`{{ROLA_AVATARA}}`, `{{ALIAS}}` i dwóch dat). Lista w INIT jest wyprowadzona z faktycznej komendy sed, nie z pamięci.
- `git add -A` w Kroku 6 nadal stoi bez sprawdzenia toplevela (scenariusz L15). Fork startuje w pustym repo, więc jest bezpieczny — ale wzmocnienie hooka to **PB-3** pkt 3.
- Stan „ZAKAZ FORKOWANIA" (BLOCKER #1 w STATE) jest **zamknięty** tym proofem.

---

**Werdykt:** PB-1 DONE. Ryzyko nieodwracalne (push na remote szablonu) wyeliminowane i zweryfikowane po obu stronach — test negatywny pokazuje szkodę, test pozytywny jej brak.
